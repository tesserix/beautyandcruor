apiVersion: v1
kind: Service
metadata:
  name: {{ include "beautyandcruor.fullname" . }}
  labels:
    {{- include "beautyandcruor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "beautyandcruor.selectorLabels" . | nindent 4 }}
